const {chromium}=require('playwright');
const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true,args:['--use-fake-ui-for-media-stream','--use-fake-device-for-media-stream']});try{const page=await browser.newPage();await page.goto('https://sai-dheeraj-237.github.io/gully/');await page.locator('#age-form input[name=age]').check();await page.locator('#age-form input[name=rules]').check();await page.locator('#age-form .btn-primary').click();await page.locator('[data-action=voice]').click();await page.locator('[data-action=record]').click();await page.locator('#audio-preview audio').waitFor({timeout:35000});assert.equal(await page.locator('#record-status').textContent(),'30s recorded');const status=await page.evaluate(()=>({recording:recording===null,stream:recordStream===null,duration:draftAudio.duration,size:draftAudio.blob.size}));assert.equal(status.recording,true);assert.equal(status.stream,true);assert.equal(status.duration,30);assert.ok(status.size>0);console.log('PASS Recording automatically stops at 30 seconds and releases the microphone');await page.locator('[data-action=record]').click();await page.waitForTimeout(500);await page.locator('[data-action=close-modal]').click();assert.equal(await page.evaluate(()=>recording===null&&recordStream===null),true);console.log('PASS Closing the composer stops active capture');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
